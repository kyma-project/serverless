apiVersion: serverless.kyma-project.io/v1alpha2
kind: Function
metadata:
    name: node-js
spec:
    env: []
    replicas: 1
    runtime: nodejs24
    runtimeImageOverride: ""
    secretMounts: []
    source:
        inline:
            dependencies: |
                {
                  "dependencies": {}
                }
            source: |
                module.exports = {
                  main: function(event, context) {
                    return 'Hello World!'
                  }
                }

