apiVersion: serverless.kyma-project.io/v1alpha2
kind: Function
metadata:
  name: nodejs24-m
  labels:
    test: performance
    app: nodejs24-m
spec:
  replicas: 1
  runtime: nodejs24
  resourceConfiguration:
    function:
      profile: M
  source:
    inline:
      dependencies: |
        {
          "dependencies": {}
        }
      source: |
        module.exports = {
          main: function(event, context) {
            console.log("Hello World!");
            return 'Hello World!'
          }
        }
---
## I'm not sure if we need this and if we should call function because this can disturbe the results of the test

apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: nodejs24-m
  labels:
    test: performance
spec:
  selector:
    matchLabels:
      serverless.kyma-project.io/function-name: nodejs24-m
  podMetricsEndpoints:
  - portNumber: 8080
    path: /metrics
    interval: 5s
