apiVersion: v1
kind: ConfigMap
metadata:
  name: k6-test
data:
  test.js: |
    import http from "k6/http";
    import { sleep } from "k6";

    export const options = {
      vus: 100,
      duration: "2m"
    };

    export default function () {
      http.get("http://f-status.default.svc.cluster.local:80/");
    }

